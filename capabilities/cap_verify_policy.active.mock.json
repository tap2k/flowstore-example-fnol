{
  "$schema": "flowstore://test/mock/v0",
  "capability_id": "cap_verify_policy",
  "variant": "active",
  "description": "Policy is active and matches the caller. Outputs bind into scope and route_verified sends the caller to incident intake.",
  "behavior": {
    "kind": "static",
    "returns": {
      "policy_active": true,
      "deductible_amount": 500,
      "named_drivers": "Casey Lin, Pat Lin"
    }
  }
}
