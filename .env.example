# Copy to .env and fill in. .env is gitignored.
#
# The test scripts read these from the ENVIRONMENT (they don't auto-load this
# file). Either export them in your shell, or source this file:
#   set -a; source .env; set +a

# Required for the Gemini-based test harness (scripts/run_*.py).
GOOGLE_API_KEY=

# How the harness invokes the flowstore compiler. Point --prefix at a flowstore
# checkout; once flowstore ships a published CLI this becomes "flowstore-compile".
# If unset, the scripts fall back to the in-monorepo npm workspace resolved two
# directories up (i.e. when this project lives under <flowstore>/examples/).
FLOWSTORE_COMPILE_CMD=npm --prefix /path/to/flowstore -w @flowstore/core run --silent flowstore-compile --
