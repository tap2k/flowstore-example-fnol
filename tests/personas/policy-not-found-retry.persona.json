{
  "$schema": "flowstore://test/persona/v0",
  "id": "policy-not-found-retry",
  "name": "Policy doesn't verify — one retry, then human escalation",
  "notes": "World-only persona. cap_verify_policy returns an inactive/unmatched policy, so the caller lands in flow_policy_not_found, retries the number once, then escalates to a human. Bound by policy-not-found-retry.",
  "mocks": {
    "cap_verify_policy": {
      "kind": "static",
      "returns": {
        "policy_active": false,
        "deductible_amount": null,
        "named_drivers": ""
      }
    },
    "cap_transfer_to_human": {
      "kind": "static",
      "returns": {
        "ok": true
      }
    }
  }
}
