{
  "$schema": "flowstore://test/persona/v0",
  "id": "coverage-question-interrupt",
  "name": "Caller asks a coverage question mid-intake (policy-question interrupt)",
  "notes": "World-only persona. Active policy so intake proceeds; the caller pauses to ask a coverage question (firing int_policy_question) then resumes. Only cap_verify_policy is mocked — cap_lookup_coverage falls through to live so the retrieve_on_turn path isn't masked. Bound by coverage-question-interrupt.",
  "mocks": {
    "cap_verify_policy": {
      "kind": "static",
      "returns": {
        "policy_active": true,
        "deductible_amount": 500,
        "named_drivers": "Casey Lin, Pat Lin"
      }
    }
  }
}
